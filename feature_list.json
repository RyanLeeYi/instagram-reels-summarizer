{
  "_note": "既有專案 2026-07-11 追補 harness。歷史上全功能可用（app.db processed_urls 69 筆至 2026-02-23），荒廢近 5 個月後重驗中：有當前 evidence 才標 passing，歷史紀錄不算數。",
  "features": [
    {
      "id": "F21",
      "name": "Antigravity CLI 影片視覺分析 backend",
      "acceptance": "新增可切換的 Antigravity 視覺分析 backend：VISUAL_ANALYZER_BACKEND=antigravity 時，Reel 仍由 FFmpeg 均勻抽取 8–10 幀，但每幀改交給 workspace 專用的 agy custom agent 做視覺理解；custom agent 僅允許 view_file、禁止 run_command/寫檔。agy 缺失、非零 exit、timeout 或空輸出時自動 fallback 到既有 Ollama frame analyzer，不讓整條 Reel 失敗；預設仍為 ollama。單元測試覆蓋 backend 選擇、agy 成功、timeout/error fallback；真實 smoke 至少一張 frame 由 agy 回傳可辨識的視覺描述；全套 pytest 通過。",
      "status": "failing",
      "signed_off": true,
      "evidence": "2026-08-10 discovery：agy 1.1.7 headless 文字正常；自訂 reels-vision agent（僅 view_file）+ --add-dir + 絕對路徑後，圖片與 MP4 都能被真實辨識。frame-per-process 方案雖已完成 adapter + fallback 單元測試，但實測單次 tool-call 可超過 45s，8–10 幀各啟 agy 會破壞 <5 分鐘目標，因此不作正式主路徑。此設計已由 F22 native-video 一次分析整支影片取代，不再推進。 2026-08-23 Ryan 於 agent-brief 回覆「我在想是不是可以直接把影片給 any cli 看，不需要逐幀分析」——確認否決 frame-per-process 主路徑，整支影片一次分析（F22，已 passing）即為採用方案。依此維持 failing + superseded_by 作為歷史紀錄，不再推進實作。註：本條仍會出現在 dispatch 的 signed failing 清單中，要讓它消失需要 harness 新增終態值（未獲核准）。",
      "superseded_by": "F22",
      "prerequisites": [],
      "touches": [
        "app/services/antigravity_visual_analyzer.py"
      ]
    },
    {
      "id": "F26",
      "name": "Telegram 指令即時切換摘要 backend",
      "acceptance": "新增 Telegram 指令 /backend：(1) 無參數時回覆目前生效的摘要 backend 名稱（ollama/claude/copilot）與該 backend 使用的模型名（OLLAMA_MODEL / CLAUDE_MODEL / COPILOT_MODEL），以及三個 backend 各自的可用性（沿用 check_summarizer_available）。(2) /backend <ollama|claude|copilot> 在不重啟服務的前提下切換摘要 backend，切換後收到的下一則連結即用新 backend 產生摘要；回覆訊息必須寫出「實際生效」的 backend——若指定的 CLI 不可用而 factory 退回 Ollama，回覆要明講退回了 Ollama，不得只回顯使用者輸入。(3) 參數不在三者之內時回覆用法說明且不改變現況。(4) 未在 TELEGRAM_ALLOWED_CHAT_IDS 內的 chat 呼叫 /backend 一律拒絕，與 /start、/status 同一套授權。(5) /status 輸出加上目前生效的摘要 backend 與模型名。(6) 切換只存在於程序記憶體，服務重啟後回到 .env 的 SUMMARIZER_BACKEND；此行為在 /backend 的回覆中註明一句。(7) 單元測試以 mock 取代真實 CLI／Ollama，覆蓋：無參數查詢、合法切換後 handler 持有的 summarizer 型別改變、CLI 不可用退回 Ollama 時回覆內容含退回說明、非法參數不改變現況、未授權拒絕；全套 pytest 通過。",
      "status": "failing",
      "signed_off": true,
      "evidence": "2026-08-31 Ryan 提出：目前摘要 backend 只能改 .env 後經 mission-control 重啟切換（F5），要改成 Telegram 指令即時切換。 2026-08-31 Ryan 簽核（/aad），acceptance 凍結。",
      "prerequisites": [],
      "touches": [
        "app/bot/telegram_handler.py",
        "app/services/summarizer_factory.py",
        "tests/test_backend_command.py"
      ]
    }
  ]
}

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Telegram Bot
python-telegram-bot>=20.7

# Instagram Download
yt-dlp>=2024.12.6
browser-cookie3>=0.19.1
instaloader>=4.15.3

# Threads (Meta) Download
# F23：metathreads 已移除——PyPI 最高只有 0.0.4，且它釘死 httpx==0.24.1，
# 與本專案的 httpx>=0.25.0 / python-telegram-bot 直接衝突（pip ResolutionImpossible）。
# Threads 下載改走既有的 Googlebot SSR / Web Scraping fallback，不需要這個套件。

# Local Whisper (faster-whisper)
faster-whisper>=1.0.0

# Ollama Local LLM
ollama>=0.4.0

# MiniCPM-V Transformers 模式 (可選)
# 如需使用 Transformers 模式，請取消以下註解
# transformers>=4.44.2
# torch>=2.0.0
# decord>=0.6.0
# scipy>=1.10.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.19.0

# Scheduler
apscheduler>=3.10.4

# HTTP Client
httpx>=0.25.0
aiofiles>=23.2.1

# Environment Variables
python-dotenv>=1.0.0

# Browser Automation (NotebookLM)
playwright>=1.40.0

# Utilities
pydantic>=2.8.0
pydantic-settings>=2.2.0
